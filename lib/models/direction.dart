class Direction {
  String distanceText;
  int distanceValue;
  String durationText;
  int durationValue;
  String encodedPoint;
  Direction(
      {this.distanceText,
      this.distanceValue,
      this.durationText,
      this.durationValue,
      this.encodedPoint});
}
